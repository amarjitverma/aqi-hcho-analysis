# ============================================================
# AQI & HCHO Hotspot Analysis - CI Requirements (Lightweight)
# Only includes packages needed to run the test suite.
# Heavy runtime packages (geemap, earthengine-api, jupyter,
# streamlit, cdsapi) are excluded since tests don't need them.
# ============================================================

# Core Data Processing
numpy>=1.24
pandas>=2.0
scipy>=1.11
statsmodels>=0.14

# Geospatial (installed via conda in CI for native GDAL support)
# geopandas, rasterio, shapely, pyproj are installed via conda
# folium is pure-python and safe to pip install
folium>=0.14

# Deep Learning
tensorflow>=2.13,<2.17

# Machine Learning
scikit-learn>=1.3

# Visualization (only what tests may transitively import)
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# Utilities
tqdm>=4.65
joblib>=1.3
pyyaml>=6.0
click>=8.1
loguru>=0.7

# Optimization
optuna>=3.3

# Model Explainability
shap>=0.42

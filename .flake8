[flake8]
max-line-length = 100
# E203: whitespace before ':' — conflicts with Black formatter's slice style
# W503: line break before binary operator — conflicts with Black
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    build,
    dist,
    *.egg-info
